{
    "name": "@sportsbook/contracts",
    "version": "1.0.0",
    "description": "Chainlink Sportsbook: x402 micro-bets on Cronos",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:testnet": "hardhat run scripts/deploy.js --network cronos-testnet",
        "deploy:mainnet": "hardhat run scripts/deploy.js --network cronos",
        "node": "hardhat node",
        "sync-abis": "hardhat export-abi"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-network-helpers": "^1.0.12",
        "@nomicfoundation/hardhat-verify": "^2.0.0",
        "@nomicfoundation/hardhat-viem": "^2.0.0",
        "hardhat": "^3.1.2",
        "viem": "^2.20.0"
    },
    "dependencies": {
        "@chainlink/contracts": "1.2.0",
        "@openzeppelin/contracts": "5.0.2"
    }
}
